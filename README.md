# MEPHI Session Project 2026

В ходе выполнения задания использовалась реальная локальная сеть хоста с подсетью `192.168.0.0/24` и шлюзом `192.168.0.1` (проверено через `ipconfig` на Windows и маршрутизацию в VM).

По этой причине проверка сетевой связности в `network_check.txt` выполнена для фактического шлюза `192.168.0.1` и внешнего адреса `8.8.8.8`.  
Маршрут и доступность в сети подтверждены реальными командами `ping`.

Остальные пункты задания (пакеты, RPM, файловая система, SELinux, capabilities, пользователи/группы, web-страница, артефакты) выполнены согласно требованиям.
